# chttpd

A lightweight HTTP server written in C.

## Project Structure
- `/src`: Source files (.c)
- `/include`: Header files (.h)
- `/tests`: Unit and integration tests
- `/public`: Static files for the server to host

## Building
Use `make` to compile the project:
```bash
make
```

## Running
Run the server:
```bash
./chttpd
```
